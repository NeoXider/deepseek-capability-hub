node_modules/
dist/
data/state/
*.log
.env
.env.*
!.env.example
.DS_Store
Thumbs.db

# Benchmark runtime state (snapshots and results are committed on purpose)
bench/state/
bench/state-described/
.playwright-mcp/

# Throwaway experiment scripts (superseded by bench/measure.mjs)
*-tmp.mjs

# Scratch scripts from audit runs
audit-scratch/

# Harness smoke receipts: timestamped, model-specific, one per run. The committed
# evidence is the reproducible kind under bench/.
artifacts/
